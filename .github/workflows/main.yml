name: Daily Gemini Blog Update

on:
  schedule:
    - cron: '0 0 * * *' # 每天北京时间早 8 点执行
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12' # 升级到 Python 3.12 消除警告

      - name: Install Dependencies
        run: pip install google-genai # 注意：包名已经改了

      - name: Run Gemini Script
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python scripts/update_blog.py

      - name: Commit and Push
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add content/posts/*.md
          git diff --quiet && git diff --staged --quiet || (git commit -m "docs: daily ai post" && git push)
